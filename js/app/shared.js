/*jshint mootools:true */
(function() {
	'use strict';

	define(function(){
		// this is a shared object namespace via require('app');

		return {
			// an example settings or any other data
			settings: {}
		};

	});
}());